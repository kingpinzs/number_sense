# Progress Feature

Progress tracking and visualization for user achievements, confidence scores, and training history.

## Structure
- `components/` - Progress visualization components (charts, history views)
- `hooks/` - Progress data fetching and analysis hooks
- `types/` - TypeScript types for progress data structures
