# Training Feature

Core training system with progressive drills targeting identified weaknesses.

## Structure
- `components/` - Training session UI components
- `drills/` - Individual drill implementations (number line, spatial, operations)
- `hooks/` - Training session state and drill management hooks
- `types/` - TypeScript types for training data structures
