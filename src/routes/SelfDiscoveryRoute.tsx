// SelfDiscoveryRoute — Self-discovery tools hub
// Symptoms checklist, personal history intake, colored dots test

import SelfDiscoveryHub from '@/features/self-discovery/components/SelfDiscoveryHub';

export default function SelfDiscoveryRoute() {
  return <SelfDiscoveryHub />;
}
